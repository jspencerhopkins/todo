<h2>Edit Task</h2>
 
<%= form_for @item do |f| %>
  <p>
    <%= f.label "Task" %><br>
    <%= f.text_field :description %>
  </p>
  <p>
  	<%= f.label "Complete?" %><br>
  	<%= f.check_box :complete %>
  </p>
  <p>
    <%= f.submit "Save"%>
  </p>
<% end %>
 
<%= link_to 'Back', root_path %>